@model RepositoryExample.Core.Estate
@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using(Html.BeginForm())
{
    @Html.LabelFor(x => x.Nombre)
    @Html.TextBoxFor(x => x.Nombre)
    <br />
    <button type="submit">Agregar</button>
}

